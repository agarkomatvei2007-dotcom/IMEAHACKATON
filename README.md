# 🇰🇿 Помощник по государственным услугам Казахстана

RAG-система на основе Gemini API для ответов на вопросы о госуслугах.

## Что умеет

- Отвечает на вопросы про получение документов
- Объясняет процедуры регистрации
- Показывает сроки и стоимость услуг
- Ссылается на источники информации
- Работает быстро и прозрачно

## Технологии

- **Gemini API** - генерация ответов
- **Sentence Transformers** - локальные эмбеддинги
- **ChromaDB** - векторное хранилище
- **Streamlit** - пользовательский интерфейс
- **RAG архитектура** - точность ответов

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка API ключа

Получите API ключ: https://makersuite.google.com/app/apikey

Создайте файл `.env`:
```bash
cp .env.example .env
```

Откройте `.env` и укажите ваш ключ:
```
GEMINI_API_KEY=ваш_ключ_здесь
```

### 3. Создание векторной базы

```bash
python create_db.py
```

Это займёт 1-2 минуты (скачает модель ~100MB при первом запуске).

### 4. Запуск приложения

```bash
streamlit run app.py
```

Откроется браузер с интерфейсом!

## База знаний

В проекте 10 документов про:
- ЭЦП
- Регистрация ИП
- Регистрация брака
- Водительские права
- Справка о несудимости
- Налоговая задолженность
- Регистрация авто
- Замена удостоверения
- Детские пособия
- Регистрация на egov.kz

## Как это работает

1. **Пользователь задаёт вопрос**
2. **Система векторизует вопрос** (локальные эмбеддинги)
3. **Ищет похожие фрагменты** в векторной базе (ChromaDB)
4. **Отфильтровывает по порогу схожести** (65%)
5. **Формирует контекст** из найденных фрагментов
6. **Gemini генерирует ответ** на основе контекста
7. **Показывает источники** для прозрачности

## Примеры вопросов

- "Как получить ЭЦП?"
- "Какие документы нужны для регистрации брака?"
- "Сколько стоит зарегистрировать ИП?"
- "Как проверить налоговую задолженность?"
- "Где получить водительские права?"

## Структура проекта

```
egov_assistant/
├── data/
│   ├── documents/          # Исходные документы (Markdown)
│   └── vector_db/          # Векторная база (создаётся автоматически)
├── src/
│   ├── embedder.py         # Генерация эмбеддингов
│   ├── generator.py        # Gemini API для ответов
│   ├── vector_store.py     # Работа с ChromaDB
│   └── rag.py              # Основная RAG логика
├── app.py                  # Streamlit интерфейс
├── create_db.py            # Скрипт создания базы
├── config.py               # Настройки
├── requirements.txt        # Зависимости
└── README.md               # Этот файл
```

## Добавление документов

Чтобы добавить новые документы:

1. Создайте `.md` файл в `data/documents/`
2. Напишите информацию в удобном формате
3. Запустите `python create_db.py` заново
4. Перезапустите `streamlit run app.py`

Готово! Система будет отвечать на вопросы по новым документам.

## Требования

- Python 3.9+
- 2GB RAM
- 1GB свободного места (для модели эмбеддингов)
- Интернет (для Gemini API)

## Возможные проблемы

### "GEMINI_API_KEY не найден"
→ Создайте файл `.env` и укажите API ключ

### "Модуль не найден"
→ Запустите `pip install -r requirements.txt`

### "Коллекция не найдена"
→ Запустите `python create_db.py`

### "Долго загружается первый раз"
→ Нормально, скачивается модель ~100MB

## Лицензия

MIT

## Автор

Проект создан для хакатона по RAG системам
